server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/liaoqichao/config-repo  # 公用的application.yml
          search-paths:
          - foo # 路径，git仓库文件夹路径
          - bar # 路径
